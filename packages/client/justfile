python_environment := justfile_directory() / "../../.env"
python_interpreter := python_environment / "bin/python"

@test:
  cd "{{justfile_directory()}}" && {{python_interpreter}} -m pytest tests/

@install:
  cd "{{justfile_directory()}}" && {{python_interpreter}} -m pip install -e .
